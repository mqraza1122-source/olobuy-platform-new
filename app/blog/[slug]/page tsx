import Link from 'next/link';
import { notFound } from 'next/navigation';
import { SiteHeader } from '@/components/site-header';
import { CtaFooter } from '@/components/cta-footer';
import { WhatsAppFloat } from '@/components/whatsapp-float';
import blogsData from '@/data/blogs.json';

interface PageProps {
  params: Promise<{ slug: string }>;
}

export async function generateMetadata({ params }: PageProps) {
  const resolvedParams = await params;
  const post = blogsData.find((p) => p.slug === resolvedParams.slug);
  if (!post) return { title: 'Post Not Found — OloBuy' };
  return {
    title: `${post.title} — OloBuy Blog`,
    description: post.excerpt,
  };
}

export default async function BlogPostPage({ params }: PageProps) {
  const resolvedParams = await params;
  const post = blogsData.find((p) => p.slug === resolvedParams.slug);

  if (!post) {
    notFound();
  }

  return (
    <div className="flex min-h-dvh flex-col bg-slate-50 text-slate-900">
      <SiteHeader />

      <main className="flex-1 px-4 py-12 md:px-8 lg:px-16 max-w-3xl mx-auto w-full">
        <div className="mb-8">
          <Link 
            href="/blog" 
            className="text-sm font-semibold text-blue-600 hover:underline inline-flex items-center gap-1 mb-6"
          >
            ← Back to Blog
          </Link>
          <div className="flex items-center justify-between text-xs font-semibold text-slate-500 uppercase tracking-wider mb-3">
            <span className="bg-blue-100 text-blue-700 px-3 py-1 rounded-full">{post.category}</span>
            <span>{post.date}</span>
          </div>
          <h1 className="text-3xl md:text-4xl lg:text-5xl font-extrabold tracking-tight text-slate-900 leading-tight">
            {post.title}
          </h1>
        </div>

        <article className="prose prose-slate lg:prose-lg max-w-none bg-white p-6 md:p-10 rounded-2xl border border-slate-200 shadow-sm leading-relaxed space-y-6">
          <div className="text-lg text-slate-700 whitespace-pre-line">
            {post.content}
          </div>
        </article>

        <div className="mt-12 bg-slate-900 text-white p-8 rounded-2xl text-center space-y-4 shadow-xl">
          <h3 className="text-2xl font-bold">Secure Your Next Online Deal with OloBuy</h3>
          <p className="text-slate-300 max-w-xl mx-auto">
            Don't risk your hard-earned money on OLX or Facebook without protection. Use Pakistan's safest manual escrow today.
          </p>
          <div className="pt-2">
            <Link 
              href="https://wa.me/923300100010" 
              target="_blank"
              className="inline-flex items-center gap-2 bg-emerald-600 hover:bg-emerald-500 text-white font-bold px-8 py-3.5 rounded-full transition-all shadow-lg text-base"
            >
              Start Free Escrow via WhatsApp 💬
            </Link>
          </div>
        </div>
      </main>

      <CtaFooter />
      <WhatsAppFloat />
    </div>
  );
}
